# BarClimb Release Notes

No production application release yet. M1.4 has a non-production Heroku staging release only.

- Pre-build specs hardened for canonical Visitor/Free/Plus architecture, server projections, orchestration models, and cross-client parity.
- Repository continuity reconciled after kickoff; v1 jurisdiction behavior clarified and Milestone 1 marked ready to begin.
- M1.1 repository/toolchain foundation added for Django/DRF, React web, Expo iOS/Android, shared TypeScript packages, and CI. No application release or business feature is included.
- M1.1a corrected that foundation to Node 24/npm 11, Django 5.2.17, one React 19.2.3 web runtime, Expo 57/RN 0.86 with TypeScript 5, deterministic Python locks, PostgreSQL-backed CI, portable-package enforcement, and documented upstream toolchain advisories. No application release or business feature is included.
- M1.2 adds Celery/Redis-Valkey runtime wiring, explicit environment contracts, Heroku process definitions, PostgreSQL+KVS readiness, structured logs, deterministic eager tests, and real worker proof. No deployment, managed provider verification, business task, or product feature is included.
- M1.3 adds the shared custom account identity, Web session/CSRF flows, provider-neutral verification/reset lifecycle, revocable native sessions with Expo SecureStore, auth throttles/permissions, and Web/iOS/Android auth proof surfaces. No learner profile, onboarding, entitlement, curriculum, assessment, billing, or provider verification is included.
- M1.3a hardens that identity slice with query-free action links, canonical Web/API origin separation, reset-safe native authentication generations, deployed HTTPS/proxy rules, a durable PostgreSQL email outbox with scheduled recovery, and deterministic native SecureStore failure states. No application release, product domain, provider verification, or M1.4 work is included.
- M1.4 adds stable Web auth routes, React Navigation auth/app boundaries, safe environment-specific client configuration, gated Universal/App Link scaffolding, and a production-rejected staging email sink. Persistent Heroku staging with managed PostgreSQL/KVS and Web/worker/beat is verified nonproduction, and a signed Android internal staging APK succeeds through EAS. The later gate reconciliation makes iOS signing/build and actual-device SecureStore/auth runtime proof Native GA—not M1.4 foundation—blockers; they remain open and unverified.
- Accepted-main dependency maintenance advances Expo SDK 57 from 57.0.13 to 57.0.15 with Expo-compatible patch-level lock refreshes while preserving React/ReactDOM 19.2.3, React Native 0.86.2, TypeScript 5.9.3, Node 24.19.0/npm 11.17.0, and application behavior.
- The 2026-08-20 controlling-spec amendment changes public distribution sequencing to Web GA → iOS Native GA → Android Native GA while preserving one first-class multi-client architecture, early native risk proof, provider-neutral entitlement, portable contracts/state, and explicit parity gates. It changes no application behavior. At amendment time M1.4 remained on its existing branch; that branch now contains amended main without history rewriting.
- The published M1.4 branch now contains accepted amended main without history rewriting. Its existing staging/Web/native-foundation implementation is preserved and reconciled to Expo 57.0.15; no later product slice is included.
- M1.4 now verifies durable ownership of and links to the existing BarClimb EAS project. Android internal build `f2b86aba-4cec-4660-b8dd-f14ea112f134` adds signed non-production build evidence only; iOS credentials/build, both platform runtimes, stores, and production remain unverified.
- M1.4 re-verification confirms `apollonomios@gmail.com` owns the healthy non-production Heroku staging app; the earlier forbidden result was an unrelated CLI account, not loss of BarClimb ownership. Native iOS configuration now explicitly declares only standard/exempt encryption. Apple build/runtime proof remains blocked because no BarClimb Apple Developer Program team exists.
- Signed Android internal build `2e340187-f441-4175-ba8b-852d044996f7` refreshes build-only evidence from exact executable commit `2e32876`; actual Android runtime/SecureStore and Google Play remain unverified.
- The M1.4/Web-first gate reconciliation defines foundation acceptance around real staging, shared auth/navigation/config/link foundations, automated SecureStore coverage, verified EAS ownership, green multi-client checks/exports, an explicit evidence ledger, and at least one signed native internal-build path where accounts permit. Apple enrollment/signing/iOS build, physical-device auth/SecureStore, live AASA/assetlinks routing, Play ownership/approval, store approvals, and native production purchase/restore move to the applicable Native GA gate; none is removed or claimed verified. This specification/continuity amendment changes no application behavior.
- M1.4 foundation acceptance is merged into accepted main at `443bbbc`; exact-main CI passed. This is not an application release or Native GA: Apple/iOS signing, actual-device auth/SecureStore, live association routing, Google Play ownership/approval, and native production purchase/restore remain open platform release blockers.
- M1.5 adds a development-only cross-client assessment presentation risk proof. One runtime-validated shared schema/state contract drives synthetic MCQ, IQS, Standard PT, and LRPT fixtures on semantic Web and React Native renderers, with answer/resource persistence, long-form formatting state, autosave transitions, interrupted-write recovery, remount restoration, responsive layout, and typed unsupported-component failure. It adds no production assessment database/API, curriculum, AI, grading, evidence, readiness, publication, or Milestone 2 work; the fixtures are hidden in production environments.
- M2.1 adds the backend foundation for immutable/versioned official-source provenance and a canonical hierarchical national `NEXTGEN_UBE` / `NEXTGEN_CORE` perimeter. It includes deterministic hashes/import, explicit validation/activation/supersession, read-only operations/API boundaries, and isolated synthetic tests. It does not import production NCBE content or add Rule Obligations, doctrine, curriculum certification, production assessment inventory, learner evidence, or AI.
- M2.2a adds a deterministic synthetic-fixture proof for versioned Rule Obligations, typed relationships, structured authority, reconciliation/coverage policy, targeted human review, certification snapshots, historical supersession, and scope-drift impact. It imports no real NCBE/legal corpus, adds no AI or assessment generation, and makes no production curriculum-completeness claim.
