# BarClimb Test Ledger

| Date | Scope | Environment | Command / Method | Result | Notes |
|---|---|---|---|---|---|
| 2026-08-12 | Continuity bootstrap | local artifact | `python3 scripts/validate_continuity.py` | PASSED | No application tests exist yet. |
## 2026-08-13 — Pre-build specification checks
- Verified four controlling Markdown specs are present and rehashed in `SPEC_MANIFEST.json`.
- Checked Build Constitution numbered headings for duplicate milestone/section numbers after milestone revision.
- Checked for stale “all nine milestone” / old Milestone 9 hardening language.
- Continuity validator must pass on the regenerated bootstrap before delivery.
- Added consistency requirement that release coverage counts only independently confirmed AssessmentScopeTargets, not GenerationSpecification tags alone.

## 2026-08-13 — Repository continuity reconciliation
- Ran `python3 scripts/validate_continuity.py`: PASSED.
- Verified `PROJECT_STATE.json` parses and records the actual baseline branch/commit.
- Verified all four controlling specifications remain present and manifest hashes match.
- Searched continuity/specification text for stale pre-kickoff initialization instructions, obsolete `python` validator invocations, and contradictory v1 jurisdiction requirements.
- Reviewed the final documentation-only diff; no application files or provider/toolchain setup were introduced.

## 2026-08-13 — M1.1 foundation verification
- `python3 scripts/validate_continuity.py`: PASSED.
- `.venv/bin/ruff check apps/backend`: PASSED; formatting check clean after scoped formatting.
- `DJANGO_SETTINGS_MODULE=config.settings.test .venv/bin/python apps/backend/manage.py check`: PASSED.
- `cd apps/backend && ../../.venv/bin/pytest`: PASSED, 2 tests (health and database readiness) on Python 3.11.2/Django 5.2.16.
- `npm run lint`: PASSED.
- `npm run typecheck`: PASSED for web, native, and all seven shared packages.
- `npm test`: PASSED, 2 Vitest smoke tests.
- `npm run build`: PASSED, Vite production bundle generated.
- `CI=1 npx expo export --platform ios`: PASSED, 575 modules bundled.
- `CI=1 npx expo export --platform android`: PASSED, 573 modules bundled.
- `npx expo install --check --json`: PASSED (`upToDate: true`).
- `npm run doctor --workspace @barclimb/native`: 16/17 locally; network-enabled package metadata check remains affected by unsupported host Node 23.1. CI targets supported Node 22.13. No simulator/device build was claimed.
- `npm audit --omit=dev`: 0 critical, 11 high, 7 moderate findings, all in the Expo/React Native toolchain graph; npm's offered fix incorrectly downgrades to Expo 53/React Native 0.72 and was not applied. Reassess with upstream SDK updates.

## 2026-08-13 — M1.1a correction verification
- Exact JavaScript runtime: Node 24.19.0 and npm 11.17.0 verified from the official Node distribution. Clean `npm ci`: PASSED; no unreviewed install scripts after pinned `esbuild@0.28.2` and `fsevents@2.3.3` review.
- `npm ls react react-dom --all`: PASSED; every resolved instance is React 19.2.3 and ReactDOM 19.2.3 is deduped. Web production bundle inspection found only the `19.2.3` React patch marker.
- `npm run check`: PASSED on Node 24.19.0/npm 11.17.0. Prettier, ESLint, all nine workspace typechecks, two web tests (including a real ReactDOM root-shell mount), one native smoke test, and Vite 7.3.6 production build passed.
- `npm run portability`: PASSED for all seven shared packages. An ESLint stdin negative probe importing `node:fs` at a shared-package path failed as expected with `no-restricted-imports`.
- `npx expo install --check`: PASSED; TypeScript 5.9.3 was explicitly skipped through `expo.install.exclude`, and all Expo SDK 57 dependencies were current.
- `npm run doctor --workspace @barclimb/native`: PASSED 20/20 with network access on Node 24.19.0.
- Expo SDK 57.0.12 production JS exports: PASSED for iOS (580 modules) and Android (578 modules). These are bundle exports, not simulator/device/store builds.
- Clean Python 3.13.15 hash-lock install: PASSED from `requirements-dev.txt`; exact packages include Django 5.2.17. Both locks were generated with pip-tools 7.6.1 under Python 3.13.15 and matched the prior candidates byte-for-byte.
- Python foundation checks: Ruff lint/format PASSED; Django SQLite system check PASSED; 2 health/readiness tests PASSED on Python 3.13.15/Django 5.2.17.
- PostgreSQL smoke: PASSED against a temporary local PostgreSQL 14 cluster on port 55432. Django system check passed, all built-in auth/contenttypes/sessions migrations applied, and 2 health/readiness tests passed with `config.settings.postgres_test`. The cluster was stopped afterward. CI targets PostgreSQL 17.
- `npm audit`: directly actionable Vite/Vitest findings were patched. `npm audit --omit=dev` retains 18 aggregate upstream Expo/React Native toolchain nodes (7 moderate, 11 high) rooted in `image-size` and `uuid`; reachability and mitigations are recorded in `SECURITY_ADVISORIES.md`.
- `python3 scripts/validate_continuity.py`: PASSED after continuity reconciliation. `git diff --check`: PASSED. Controlling specifications and `SPEC_MANIFEST.json` were unchanged.
- Native simulator/device/store build: NOT VERIFIED. Full Xcode is absent; Android SDK/adb and EAS are not configured.

## 2026-08-13 — M1.2 runtime/environment verification
- Clean Python 3.13.15 `pip --require-hashes` install from regenerated pip-tools 7.6.1 locks: PASSED; Celery 5.6.3 and Redis client 6.4.0 resolved.
- Ruff lint/format, Django test-settings system check, and 9 SQLite deterministic tests: PASSED. Coverage includes malformed environment values, HTTPS-origin validation, missing production configuration, eager Celery discovery/execution, and required-KVS readiness failure.
- Temporary PostgreSQL 14 + Redis 7.2 integration: migrations PASSED; 9 tests PASSED through `config.settings.postgres_test`; readiness reported database/KVS round trips healthy.
- Live Celery 5.6.3 solo worker against Redis 7.2: PASSED. Worker loaded Django configuration, reported `results: disabled`, discovered only `infrastructure.smoke`, and executed the published task successfully.
- `npm run check`: PASSED; Prettier, ESLint, all workspace typechecks, 3 Vitest tests, and Vite production build passed.
- `npm run portability`: PASSED for all seven shared packages.
- `python3 scripts/validate_continuity.py`: PASSED. `git diff --check`: PASSED. Four controlling specs and manifest remained unchanged.
- CI workflow now includes PostgreSQL 17, Redis 7.2, backend integration tests, and live worker smoke; GitHub Actions result pending branch push.

## 2026-08-13 — M1.3 identity/authentication verification
- Local SQLite path on the available Python 3.11 host: Django system check and 25 Pytest tests PASSED. Identity coverage includes user creation, deterministic email/username normalization and uniqueness, reserved-name rejection, password hashing/authentication, Web CSRF failure/success, session rotation, authenticated lookup, logout invalidation, generic reset responses, weak-password rollback, expiring/purpose-bound/rotating/single-use token behavior, password reset and native-session revocation, native signup/login/restoration/expiry/revocation, auth throttling, no-store private responses, deployed email fail-closed behavior, anonymous denial, and cross-user ownership rejection.
- Production-settings subprocess test PASSED for HTTPS redirect, HSTS, secure/HttpOnly session and CSRF cookies; missing mandatory production configuration still fails closed.
- `npm run check` and `npm run portability`: PASSED after updating the Web shell test; Prettier, ESLint, all workspace typechecks, four Web/native Vitest tests, and Vite build. Native SecureStore wrapper testing proves opaque-token save/restore/delete behavior through an injected platform-storage boundary.
- `npx expo install --check`: PASSED with Expo SecureStore 57.0.1 current for SDK 57. Expo Doctor: PASSED 20/20 with network access. Local production JS exports PASSED for iOS (584 modules) and Android (582 modules); these are bundles, not simulator/device/store builds.
- `npm audit --omit=dev`: unchanged 18 aggregate upstream Expo/React Native toolchain findings (7 moderate, 11 high) from the two documented `image-size`/`uuid` sources; the offered incompatible Expo 53 downgrade was not applied.
- GitHub Foundation CI: PASSED all continuity, backend, and TypeScript jobs on the pushed M1.3 branch. The backend job used exact Python 3.13.15 with PostgreSQL 17 + Redis 7.2, applied the custom-user migration, passed all 25 tests, and passed live Celery worker smoke. The TypeScript job used Node 24.19.0, clean `npm ci`, full checks/portability, Expo compatibility/Doctor, and iOS/Android production JS exports. Local device/simulator keychain behavior and real transactional-email delivery remain unverified.

## 2026-08-13 — M1.3a authentication-boundary verification
- `.venv/bin/ruff check apps/backend`: PASSED. `manage.py makemigrations --check --dry-run --settings=config.settings.test`: PASSED, no model/migration drift.
- `.venv/bin/python -m pytest apps/backend/tests -q`: PASSED, 48 tests; four PostgreSQL-only concurrency tests skipped by design. Coverage includes query-free canonical-Web links, POST-only completion, Web-session and multi-device reset invalidation, Bearer variants, Heroku IPv4/IPv6/forged-chain handling, casing-resistant throttles, KVS generic fail-closed behavior, durable outbox publication/retry/duplicate/lease/key-rotation recovery, and HTTPS redirect/forwarding behavior.
- Temporary PostgreSQL 14 + Redis 7.2 authoritative run through `config.settings.postgres_test`: PASSED, all 52 backend tests. Both native-login/reset lock orders, token consume/reissue serialization, and duplicate-signup uniqueness raced in separate database connections; durable outbox worker locking/retry also ran under PostgreSQL semantics. Both temporary services were stopped afterward.
- `npm run lint`, `npm run typecheck`, and `npm test`: PASSED. Web has four Vitest tests including both fragment completion routes/history cleanup; native has 13 tests including authoritative rejection, offline/5xx preservation, SecureStore read/write/delete failures, server-revocation outcomes, and successful restoration/logout.
- `npm run check`: PASSED; Prettier, ESLint, all workspace typechecks, 17 Web/native Vitest tests, and the Vite production build passed. `npm run portability`: PASSED for all seven shared packages.
- Expo Doctor: PASSED 20/20. Dependency check reported current from the bundled offline map (TypeScript remains intentionally excluded). Fresh production JS exports PASSED for iOS (584 modules) and Android (582 modules); these remain bundle proofs, not simulator/device/store builds.
- `python3 scripts/validate_continuity.py`: PASSED. `git diff --check`: PASSED. The four controlling specs and `SPEC_MANIFEST.json` remain unchanged.
- Hosted Foundation CI: PASSED continuity, backend, and TypeScript jobs. The backend job used Python 3.13.15, PostgreSQL 17, Redis 7.2, hash-locked dependencies, Ruff lint/format, migrations, all 52 tests, and live Celery worker smoke. The TypeScript job used Node 24.19.0, clean `npm ci`, full checks/portability, Expo dependency/Doctor checks, and iOS/Android exports. Real Heroku routing/proxy headers, real transactional-email provider delivery/idempotency, native device SecureStore/keychain behavior, universal/app links, and simulator/store builds remain unverified.

## 2026-08-15 — Accepted-main Expo patch continuity correction
- Expo authority: `npx expo install --fix --npm` selected direct `expo@~57.0.13`; subsequent CI-mode dependency validation reported `Dependencies are up to date`. React Native remained 0.86.2 and TypeScript remained 5.9.3 under `expo.install.exclude`.
- Exact-runtime install: clean `npm ci` PASSED under Node 24.19.0/npm 11.17.0; 687 packages installed from the regenerated lock.
- Expo Doctor: PASSED 20/20. iOS production export PASSED (585 modules); Android production export PASSED (583 modules). These remain bundle exports, not device/store builds.
- `npm run check`: PASSED, including Prettier, ESLint, all workspace typechecks, 4 Web tests, 13 native tests, and the Vite production build. `npm run portability`: PASSED for all seven shared packages.
- Backend regression: Ruff lint/format and Django system check PASSED; 48 SQLite tests PASSED with four PostgreSQL-only concurrency tests skipped by design. The dependency-only correction does not alter backend dependencies or behavior.
- `npm audit --omit=dev`: unchanged 18 aggregate nodes (7 moderate, 11 high) rooted in the same `image-size` and `uuid` upstream toolchain advisories; Expo-owned path versions were refreshed in `SECURITY_ADVISORIES.md`. No forced/incompatible fix was applied.
- Continuity validation and `git diff --check`: PASSED locally. Controlling specifications and `SPEC_MANIFEST.json` remain unchanged. Hosted Foundation CI on the exact correction commit is the remaining acceptance observation at commit time.

## 2026-08-15 — M1.4 staging/auth/client proof (in progress)
- Preflight: clean accepted `main` at `fa476bff604578c0825a70db61b1d5ef7dee3b2d`, equal to `origin/main`; GitHub Actions run `31892309877` passed all three jobs. Continuity validation passed before the slice.
- Exact Node 24.19.0/npm 11.17.0 clean dependency install passed after lock regeneration. Expo-compatible `react-native-safe-area-context@5.7.0` and `react-native-screens@4.26.2` resolve once each. `CI=1 npx expo install --check` reported current; Expo Doctor passed 20/20 with network.
- `npm run check`: PASSED with all nine workspace typechecks, lint/format, 9 Web tests, 18 native tests, and production Web build. `npm run portability`: PASSED. iOS export passed at 854 modules; Android export passed at 849 modules. These remain JS bundle proofs, not internal builds.
- Backend Ruff lint/format: PASSED. SQLite test suite: 60 passed, 4 PostgreSQL-only tests skipped. New coverage includes deployed-environment validation, staging sink safety, direct Web routes/assets, association publication gating, environment selection, and canonical-link credential redaction.
- Heroku code release v8 deployed commit `6bec558`: Node 24.19.0/npm 11.17.0 Web build, Python 3.13.15 hash-locked install, collectstatic, deployment check, and migrations passed. Web, worker, and beat each ran one Basic dyno. After staging PostgreSQL, KVS, and Django signing credentials were rotated, current configuration release v12 restarted all three processes and `GET /api/v1/ready/` again returned 200 with database and KVS both `ok`.
- `GET /api/v1/health/`: 200. `GET /api/v1/ready/`: 200 with database and KVS both `ok`. Direct HTTP root returned 301 to HTTPS; HTTPS root/auth destinations returned 200. Direct/refresh routes verified: login, signup, verify-email, forgot-password, reset-password, and app.
- Controlled Web proof: signup 201, authenticated session true, login 200, authenticated session true. The first scripted logout used the pre-login CSRF token and correctly failed 403 after Django rotated the CSRF secret; automated local logout-rotation coverage remains green.
- Proxy/rate-limit proof: 11 native-login failures used 11 different caller-supplied `X-Forwarded-For` values and unique identities. Results were ten 400 responses followed by 429, proving the Heroku-appended rightmost address—not caller-prepended spoof values—owned the shared IP counter.
- Native API proof: staging issued a bearer for a generated account, accepted revocation with 204, then returned 401. One controlled diagnostic printed that test bearer; it was immediately revoked before further work. This is backend protocol evidence, not native-client/runtime evidence.
- Outbox proof: staging aggregate showed one verification delivery in `SENT`. Logs showed beat publishing the one-minute recovery task, worker pickup/success, delivery-task pickup, and `staging auth email accepted action=/verify-email`. A 1,500-line app-log scan found zero fragment/query-token, Authorization, Bearer, or password-shaped credential patterns.
- Universal/App Link endpoints returned 404 on staging because `MOBILE_LINKS_ENABLED=false`; unit tests prove the gated payload shape and auth-route exclusion. No OS/domain association is claimed.
- Local platform probe: `xcodebuild` reports only Command Line Tools; `simctl` is unavailable; Android SDK, `adb`, and emulator are absent. EAS `whoami` reported `Not logged in`; final noninteractive iOS and Android internal-build attempts both stopped with `An Expo user account is required`. Internal builds, simulator/emulator/device auth, and actual SecureStore read/write/delete/restart/uninstall behavior are NOT VERIFIED.
- `npm audit --omit=dev`: 18 aggregate nodes (7 moderate, 11 high), unchanged from the documented `image-size` and `uuid` upstream toolchain roots; no compatible fix appeared. Final clean `npm ci`, full checks/portability, Expo validation, Doctor 20/20, both exports, 60 backend tests, continuity validation, and diff checks passed.
- GitHub Foundation CI run `31895220658`: PASSED on exact commit `68ef81a2d4ec7cdbfb4290c046c221ad9d0f2016`. Continuity passed; backend used PostgreSQL 17 + Redis 7.2, passed all 64 tests and live Celery smoke; TypeScript used Node 24.19.0/npm 11.17.0 and passed clean install, full checks/portability, Expo validation/Doctor, and both JS exports.
- GitHub Foundation CI run `31895608020`: PASSED on exact commit `0be37893a0660712962e1d2ec14fdf4d1c836339`, including the completed native placeholder route registry. All continuity, backend/PostgreSQL/Redis/Celery, and TypeScript/Expo jobs passed.

## 2026-08-20 — Web-first release-strategy specification reconciliation
- Preflight: accepted `main` was clean at `fa476bff604578c0825a70db61b1d5ef7dee3b2d`, equal to `origin/main`; continuity validation passed before creating `spec-web-first-release-strategy`. The existing `m1-4-staging-auth-client-proof` branch was left untouched and unmerged.
- Four controlling specifications were reconciled from the supplied authoritative documents. The Build/Native copies additionally corrected supplied-text contradictions with durable national-v1, provider-neutral entitlement, independent Native GA, multi-client portability, and three-scenario requirements. Manifest SHA-256 values were regenerated for all four files.
- `python3 -m json.tool docs/specs/SPEC_MANIFEST.json` and `docs/project/PROJECT_STATE.json`: PASSED.
- `python3 scripts/validate_continuity.py`: PASSED, including exact manifest/checksum verification.
- Ten-milestone sanity: PASSED; exactly ten ordered Milestone headings are present, Milestone 1 through Milestone 10 at Constitution §§103–112.
- Contradiction searches: PASSED for obsolete simultaneous/coordinated-public-launch requirements, coupled iOS/Android Native GA gating, jurisdiction-dependent v1 onboarding, and Stripe-specific canonical entitlement. Historical superseded decisions are explicitly labeled rather than silently erased.
- `git diff --check`: PASSED. Full diff review found only controlling specification, manifest, recovery, and project-continuity changes; no application, dependency, environment, provider configuration, deployment, or M1.4 file changed.
- GitHub Foundation CI run `32413722581` on the first pushed amendment commit: continuity PASSED; backend/PostgreSQL/Redis/Celery PASSED; TypeScript `npm ci`, `npm run check`, and portability PASSED. `npx expo install --check` FAILED because Expo's live SDK 57 metadata now expects `expo@~57.0.15` while accepted main pins `~57.0.13`; Doctor and iOS/Android exports were skipped after the dependency gate. The spec amendment did not touch dependency files. Record this as external accepted-baseline drift requiring a separate focused correction, not permission to expand this branch.

## 2026-08-20 — Accepted-main Expo SDK 57 patch follow-up
- Preflight: clean accepted `main` at `fa476bff604578c0825a70db61b1d5ef7dee3b2d`, equal to `origin/main`; continuity passed before creating `maintenance-expo-sdk57-patch`. `spec-web-first-release-strategy` remained `3236bfc` locally/remotely and `m1-4-staging-auth-client-proof` remained `fa5b2e7` locally/remotely.
- Exact Node 24.19.0/npm 11.17.0 correction: Expo direct dependency changed `~57.0.13` → `~57.0.15`; clean `npm ci` installed 687 packages. `CI=1 npx expo install --check`: PASSED against live metadata. Expo Doctor: PASSED 20/20.
- Companion lock refreshes: `@expo/cli` 57.0.15 → 57.0.17 and deduplicated; `@expo/config` 57.0.7 → 57.0.8; `@expo/fingerprint` 0.20.7 → 0.20.9; `@expo/inline-modules` 0.1.5 → 0.1.6; `@expo/local-build-cache-provider` 57.0.6 → 57.0.7; `@expo/log-box` 57.0.2 → 57.0.3; `@expo/metro-config` 57.0.8 → 57.0.9; `@expo/prebuild-config` 57.0.12 → 57.0.13; `expo-asset` 57.0.11 → 57.0.13; `expo-constants` 57.0.11 → 57.0.13; `expo-file-system` 57.0.4 → 57.0.5; `expo-modules-core` 57.0.11 → 57.0.12; `expo-modules-jsi` 57.0.4 → 57.0.5; `agent-cli-detector` 0.1.5 → 0.1.6. No React, React Native, SecureStore, or TypeScript version changed.
- `npm run check`: PASSED with formatting/lint, all nine workspace typechecks, 4 Web tests, 13 native tests, and production Web build. `npm run portability`: PASSED for all seven shared packages.
- Production JS exports from the native workspace: iOS PASSED at 585 modules; Android PASSED at 583 modules. These remain bundle exports, not device/store builds.
- Backend accepted-baseline regression on available Python 3.11.2: Ruff lint/format, Django system check, and migration drift check PASSED; 48 tests PASSED with four PostgreSQL-only concurrency tests skipped by design.
- `npm audit --omit=dev`: same two advisory families (`image-size`, `uuid`), with aggregate nodes reduced from 18 (7 moderate, 11 high) to 15 (7 moderate, 8 high) after Expo CLI deduplication. No forced fix or new advisory family.
- Project-state JSON parsing, continuity validation, `git diff --check`, controlling-spec isolation, and final secret/generated-junk review: PASSED locally. Hosted Foundation CI on the exact maintenance commit remains the final acceptance observation at commit time.

## 2026-08-20 — Web-first amendment accepted-main integration
- Strict accepted-main fast-forward `fa476bf` → `77d5567`: PASSED; local `main` and `origin/main` matched exactly with a clean tree. GitHub Foundation CI run `32415566577` PASSED continuity, backend/PostgreSQL/Redis/Celery, TypeScript checks, live Expo validation/Doctor, and iOS/Android exports on the exact main commit.
- Normal merge of `origin/main` into `spec-web-first-release-strategy` preserved published history. Four continuity conflicts were reconciled; controlling specs and manifest retained the amendment, while Expo 57.0.15, its lock refresh, security advisory evidence, and accepted implementation came from main.
- Exact Node 24.19.0/npm 11.17.0 post-merge regression: clean `npm ci` PASSED with 687 packages; live `CI=1 npx expo install --check` PASSED; Expo Doctor PASSED 20/20; `npm run check` PASSED formatting/lint, all nine workspace typechecks, 4 Web tests, 13 native tests, and the Web production build; `npm run portability` PASSED all seven shared packages.
- Manifest/project-state JSON parsing, exact spec checksums, ten ordered milestone sanity, obsolete simultaneous-launch search, v1 jurisdiction-regression search, Stripe-specific entitlement-truth search, continuity validation, merge diff review, and `git diff --check`: PASSED. Historical launch language remains only where explicitly marked superseded. No application behavior or M1.4 file changed.

## 2026-08-20 — M1.4 accepted-main integration and completion audit
- Normal merge of accepted main `4850b78` into `m1-4-staging-auth-client-proof` preserved published history, all amended controlling specifications/manifest, the existing M1.4 implementation/evidence, and the Expo SDK 57.0.15 baseline.
- The conflicted native declaration retained M1.4 React Navigation and `expo-linking` while adopting Expo `~57.0.15`; the npm lock was regenerated under Node 24.19.0/npm 11.17.0. Verification and current external-account evidence follow below.
- Current staging probe: public health and readiness returned 200 with database/KVS `ok`; HTTPS root returned 200 and HTTP root redirected 301 to HTTPS. Both association endpoints remain intentionally unpublished at 404. The authenticated Heroku account returned `forbidden` for `barclimb-staging`, and the app was absent from its app list, so current process/add-on/account control cannot be refreshed without human access restoration.
- Expo/EAS: browser authentication completed; `whoami` verified `opusars` and `opusarss-team` Owner roles. The repository linked successfully to existing project `@opusarss-team/barclimb`, ID `8ed30301-53d9-4630-b2c4-70d6d51f465b`; no duplicate project was created.
- Local platform/credential probe: full Xcode and `simctl` remain absent, zero valid Apple code-signing identities are available, Android SDK variables/`adb`/emulator are absent, and no local Android keystore was found. These are host limitations, not claims about EAS-managed credentials.
- Post-merge dependency/runtime regression: clean npm install from the regenerated lock passed; live Expo validation required and applied only `expo-linking` `~57.0.6` → `~57.0.7`; Expo Doctor then passed 20/20. `npm run check` passed all nine typechecks, 9 Web tests, 18 native tests, and Web build; portability and iOS/Android JS exports (854/849 modules) passed. Ruff lint/format, Django system/migration checks, and 60 SQLite tests passed with four PostgreSQL-only skips.
- Exact-runtime final rerun: official Node 24.19.0/npm 11.17.0 clean `npm ci` installed 717 packages; live `CI=1 npx expo install --check` reported current with the controlling TypeScript exclusion; Expo Doctor passed 20/20; `npm run check` passed all nine typechecks, 9 Web tests, 18 native tests, and Web build; all seven portability package checks passed. Fresh iOS and Android production JS exports passed at 854 and 849 modules. Ruff lint/format, Django system/migration drift checks, and 60 local backend tests passed with four PostgreSQL-only skips.
- Android internal build: PASSED. EAS build `f2b86aba-4cec-4660-b8dd-f14ea112f134`, profile/distribution `internal`, application ID `com.barclimb.app.staging`, version code 1, produced a signed APK from exact commit `9174007d968bc444c62126b4590793b58ca571b3`. Fingerprint: `1ad959e353189daafca947a6be72df3c6759da9a`. Evidence: `https://expo.dev/accounts/opusarss-team/projects/barclimb/builds/f2b86aba-4cec-4660-b8dd-f14ea112f134`. This proves EAS-managed Android build/signing only; no emulator/device/auth/SecureStore or Google Play claim follows.
- iOS internal build: BLOCKED before upload. The linked project has no suitable iOS internal-distribution credentials and noninteractive EAS cannot create/authorize them. Full Xcode/simctl and valid local Apple code-signing identities are also absent. The smallest human action is an authorized Apple Developer account holder running the internal build interactively, completing Apple login/2FA, team selection, staging bundle registration/verification, distribution-certificate/provisioning-profile authorization, and test-device registration without sharing credentials.
- Native runtime/SecureStore: NOT VERIFIED on either platform. Android SDK/adb/emulator and full Xcode/simctl are absent; no physical device was available. Deep-link configuration and canonical parsing are test-covered, but staging AASA/assetlinks remain 404 with `MOBILE_LINKS_ENABLED=false`, so no OS association/routing is claimed.
- `npm audit --omit=dev`: 15 aggregate nodes (7 moderate, 8 high), unchanged from the accepted `image-size` and `uuid` upstream toolchain families; no new family, path/count change, forced fix, or security-ledger update was required.
- Heroku account correction: the original forbidden probe used unrelated CLI identity `leor@cashcopawn.com`. After logout and fresh browser authentication, `heroku auth:whoami` returned owner `apollonomios@gmail.com`; app metadata and access list identify that account as sole owner. Web, worker, and beat are each `up` on Basic dynos/release v12. Essential-0 PostgreSQL 18.3 is `Available`; Mini KVS 8.1.9 is `available` with TLS; public health/readiness/HTTPS/redirect checks remain green. The read-only account-delinquency API returned null scheduled suspension/deletion. No provider mutation occurred.
- iOS export configuration: generated staging Expo config resolves bundle `com.barclimb.app.staging` and `ITSAppUsesNonExemptEncryption=false`. This matches current HTTPS/OS-backed SecureStore-only behavior; no proprietary cryptography was found. Apple team/signing/build remains blocked because no BarClimb Apple Developer Program team exists.
- Post-correction regression on exact Node 24.19.0/npm 11.17.0: clean `npm ci` installed 717 packages; live Expo compatibility reported current; Doctor passed 20/20; `npm run check` passed all nine typechecks, 9 Web tests, 18 native tests, and Web production build; all seven portability packages passed; fresh iOS/Android JS exports passed at 854/849 modules. Ruff lint/format, Django system/migration checks, and 60 local backend tests passed with four PostgreSQL-only skips. Continuity, manifest/JSON, ten-milestone, diff, and secret/junk checks passed. Audit remained 15 aggregate nodes (7 moderate, 8 high) in the documented `image-size`/`uuid` families, so `SECURITY_ADVISORIES.md` did not change.
- Exact-executable-state Android internal build: PASSED. EAS build `2e340187-f441-4175-ba8b-852d044996f7`, profile/distribution `internal`, package `com.barclimb.app.staging`, version code 1, used existing EAS-managed keystore `Build Credentials 0f5B5Nvpa2` and produced a signed APK from commit `2e32876bce0ecf315b174071fa6505d408f29258`. Fingerprint: `ac189766650e9cb44f0864814b6f46d26915d4ae`. Evidence: `https://expo.dev/accounts/opusarss-team/projects/barclimb/builds/2e340187-f441-4175-ba8b-852d044996f7`. This remains build-only evidence; no Android runtime, SecureStore, or Google Play claim follows.

## 2026-08-20 — M1.4/Web-first foundation-gate reconciliation
- Preflight: clean accepted `main` and `origin/main` were both `4850b7829e9a5f6205082a761643d6f109de7fd6`; exact-sha Foundation CI runs `32416558206` and `32417562112` were completed/success, and continuity passed before `spec-m14-web-first-gate-reconciliation` was created. `m1-4-staging-auth-client-proof` remained untouched locally/remotely at `4c1d2e64a0ab2e29684fcf95599ab2c4e27cf769`; exact-sha Foundation CI run `32423925699` was completed/success.
- `python3 scripts/validate_continuity.py`: PASSED, including all four controlling specification files and exact manifest SHA-256 values.
- `python3 -m json.tool` for `SPEC_MANIFEST.json` and `PROJECT_STATE.json`: PASSED.
- Ten-milestone sanity: PASSED; Constitution §§103–112 contain exactly ordered Milestones 1 through 10.
- Contradiction review/search: only explicitly superseded coordinated/simultaneous-launch history remains; national NextGen v1 still forbids jurisdiction-dependent onboarding/recommendations/analytics/generation/readiness; Stripe remains a purchase source rather than canonical entitlement truth.
- Native-gate retention search/review: PASSED. The controlling bundle still requires per-platform signing/build, physical-device SecureStore/auth lifecycle, AASA/assetlinks publication and actual Universal/App Link routing, store ownership/approval, and purchase/restore at the applicable Native GA. Android signing evidence is expressly non-transferable to iOS or runtime/link/store proof.
- `git diff --check`: PASSED. Scope isolation found only `RECOVERY_START_HERE.md`, `docs/specs/*`, and `docs/project/*`; no application, package/dependency, provider configuration, deployment, or M1.4 branch file changed.
- Full specification/continuity diff review: PASSED; M1.4 foundation acceptance and Native GA obligations use consistent evidence-status semantics. GitHub Actions remains pending until the exact reconciliation commit is pushed.

## 2026-08-20 — M1.4 foundation acceptance revalidation
- Merge: authoritative main `95b2d269566aae8a16045341d983b396c2a7a717` merged normally into M1.4; nine continuity-only conflicts reconciled. No application/dependency conflict or history rewrite.
- Exact runtime: Node 24.19.0/npm 11.17.0; clean `npm ci` PASSED with 717 packages.
- Live `CI=1 npx expo install --check`: PASSED. Expo Doctor: PASSED 20/20.
- `npm run check`: PASSED formatting/lint, all nine workspace typechecks, 9 Web tests, 18 native tests, and production Web build. `npm run portability`: PASSED all seven shared packages.
- Production JS exports: iOS PASSED at 854 modules; Android PASSED at 849 modules.
- Backend on local Python 3.11.2: Ruff lint/format PASSED; Django system and migration-drift checks PASSED; 60 tests PASSED with four PostgreSQL-only tests skipped. Exact-commit GitHub CI remains the PostgreSQL 17/Redis 7.2/Celery acceptance path.
- Audit: `npm audit --omit=dev` returned the unchanged 15 aggregate nodes (7 moderate, 8 high) from the documented `image-size` and `uuid` families. No new family/path/count and no forced fix.
- Staging: `apollonomios@gmail.com` verified as owner. Web/worker/beat were up; Essential-0 PostgreSQL 18.3 and Mini KVS 8.1.9 were available; health/readiness returned 200 with database/KVS `ok`; HTTPS root/login/signup/app returned 200; HTTP redirected 301; anonymous auth returned 401; AASA/assetlinks remained intentional 404. No mutation occurred.
- EAS: authenticated Owner access to `@opusarss-team/barclimb` project `8ed30301-53d9-4630-b2c4-70d6d51f465b` PASSED. Build `2e340187-f441-4175-ba8b-852d044996f7` remained finished/internal/Android at executable commit `2e32876bce0ecf315b174071fa6505d408f29258`.
- Continuity/manifest hashes, JSON, exactly ten milestones, release/Native-GA contradiction searches, Native GA evidence-retention searches, `git diff --check`, secret/generated-junk review, and final scope/diff review: required before commit; exact-commit GitHub CI pending push.

## 2026-08-20 — M1.5 cross-client assessment presentation proof
- Preflight: continuity PASSED on clean accepted main `443bbbc849049d515d9c9b1797cd7909f50359f9`, equal to `origin/main`; exact-main Foundation CI run `32427862224` was completed/success before branch `m1-5-assessment-presentation-proof` was created.
- Exact runtime: disposable Node 24.19.0/npm 11.17.0; clean `npm ci` PASSED with 717 packages.
- Live Expo compatibility: `CI=1 npx expo install --check` PASSED. Expo Doctor PASSED 20/20.
- `npm run check` PASSED formatting/lint, all nine workspace typechecks, 14 Web tests, 20 native tests, 8 shared assessment-schema/state tests, and the Web production build. Web tests cover semantic MCQ reselection/review, IQS resource switching without answer loss, and recoverable interrupted-save presentation. Native tests cover registry support/fail-safe plus the existing auth/link/environment suite; native concrete UI is typechecked and export-compiled without claiming device execution.
- `npm run portability` PASSED all seven shared packages. The assessment package remains ES2022-only and lint-rejected from DOM, React, React Native, Node globals, and client renderer imports.
- Fresh production JS exports PASSED: iOS 863 modules; Android 858 modules. Exports are compile/bundle evidence, not device or Native GA evidence.
- Backend regression on available Python 3.11.2: Ruff lint/format, Django system check, and migration-drift check PASSED; 60 tests PASSED with four PostgreSQL-only concurrency tests skipped locally. Exact-commit hosted CI remains the PostgreSQL 17/Redis 7.2/Celery path.
- `npm audit --omit=dev`: unchanged 15 aggregate nodes (7 moderate, 8 high) in the documented `image-size` and `uuid` Expo/toolchain families. No new family/path/count, forced fix, or security-ledger change.
- Continuity, manifest and project-state JSON, exact controlling-spec hashes, exactly ten milestones, controlling-spec isolation, `git diff --check`, lockfile scope review, and secret/generated-junk review PASSED. Pre-existing ignored `.DS_Store` files were not added or modified. Exact-commit GitHub Actions remains pending push.

## 2026-08-21 — M2.1 immutable official-source/scope foundation
- Preflight: continuity PASSED on clean accepted main `0ebccf8da5e08fe18079e5d59477f33a956ae88b`; exact-main Foundation CI run `32434901406` was completed/success before bounded branch creation.
- Backend: Ruff, Django system/migration checks, and 73 SQLite tests PASSED with six PostgreSQL-only skips. An isolated PostgreSQL 14/Redis 7.2 run passed all 79 backend tests; the focused M2.1 PostgreSQL suite passed 15/15, including database-trigger rejection of queryset artifact, item, and active-scope mutation. Migration application and Celery `infrastructure.smoke` passed.
- Focused tests cover deterministic SHA-256/canonical JSON, duplicate/changed import, metadata identity, immutable artifacts, multiple source roles, hierarchy/leaves/treatment, missing parent, duplicate item IDs, national-only rejection, unresolved source mapping, explicit fixture-rejected activation, validated supersession/history, failed-activation rollback, authentication/read-only API, fixture API exclusion, dry-run rollback, and PostgreSQL historical triggers.
- Exact Node 24.19.0/npm 11.17.0 clean `npm ci` installed 717 packages. `npm run check` passed nine typechecks, 14 Web tests, 20 native tests, 8 assessment-schema tests, and Web build; seven-package portability passed. Expo dependency validation and Doctor 20/20 passed; iOS/Android production JS exports passed at 863/858 modules.
- `npm audit --omit=dev` remains the accepted 15-node aggregate (7 moderate, 8 high) in the documented `image-size`/`uuid` families; no forced fix or security-ledger update. The Python lock is generated for exact Python 3.13: the available 3.11 disposable environment correctly exposed `psycopg`'s 3.11-only extra as outside that hash lock, while exact Python 3.13 clean install remains a hosted-CI gate.
- Final continuity/JSON/manifest/diff/secret review and exact-commit GitHub CI remain pending.

## 2026-08-21 — M2.2a deterministic Rule Obligation compiler proof
- Preflight: continuity PASSED on clean accepted main `1b3ee5996cf82c50f3b645e1c29831115023dbb1`; exact-main Foundation CI run `32441068155` passed before bounded branch creation.
- Clean exact-runtime Python gate: hash-verified dependency install on Python 3.13.15 PASSED. Ruff lint/format, Django system checks, and migration consistency PASSED. SQLite ran 85 tests with eight PostgreSQL-only skips; PostgreSQL 14 ran all 93 tests with no skips. The live Redis/Celery `infrastructure.smoke` task PASSED.
- Focused coverage includes 12 deterministic compiler tests plus two PostgreSQL-only integrity tests: all eleven kinds, normalization/hash/idempotency, many-to-many leaf mapping, typed/circular-invalid relations, structured primary-vs-secondary provenance, every reconciliation category and severity architecture, deterministic approval/review/block decisions, California/state-constitution/local-procedure rejection, certification block/success/rollback, immutable snapshot/history, staff review authorization, added/removed/changed scope drift, production fixture exclusion, authenticated read-only API, and dry-run command rollback/state reporting.
- Exact Node 24.19.0/npm 11.17.0 clean `npm ci` installed 717 packages without lockfile change. `npm run check` PASSED all nine workspace typechecks, 14 Web tests, 20 native tests, 8 assessment-schema tests, and the Web production build. Seven-package portability PASSED. Expo dependency validation reported current; Doctor passed 20/20; iOS/Android production JS exports passed at 863/858 modules.
- `npm audit --omit=dev` retained 15 aggregate nodes and the same two documented `image-size`/`uuid` families and dependency paths. Current registry metadata reports 10 moderate/5 high instead of the prior 7 moderate/8 high; this metadata-only severity redistribution is reconciled in `SECURITY_ADVISORIES.md`. No dependency, mitigation, advisory family, lockfile, or application behavior changed; no forced fix ran.
- Continuity validation, project/manifest JSON parsing, controlling-spec hashes, exactly ten milestone headings, controlling-spec and Web/native/dependency isolation, `git diff --check`, secret/generated-junk scan, and final scope review PASSED. Exact-commit GitHub CI remains pending push.

## 2026-08-21 — M2.2b V2 second-review correction
- Focused real-source/pilot tests: 12 passed. Regression coverage fixes Candidate 2 as `LIMITATION`, forbids Candidate 1's removed edges, preserves Candidate 3's Rule 4(h) addressee distinction, requires both Candidate 4 location predicates and attorney-fee expense detail, enforces Candidate 5's exact forum-state/service-state formulation, preserves Candidate 6→7 and 6→8 relationships, keeps all eight `REVIEW_REQUIRED`, blocks certification without attestations, and keeps `national_complete` false.
- Exact acquired-byte isolated proof: current NCBE scope validates/activates at `2d8a1052ada18b413f24b7d0eef1c855a76d8a9a31688130757d5dd6511ca56f`; V2 compiler output is `0148dea24c906e2e257265681044ae57ad4b60b9a1e290f291e95dc2315825ec`. Candidate/obligation count 8/8; every issue category and severity count is zero; one of one pilot leaves is sufficient; 20 active testable leaves remain outside any national-completeness claim.
- Ruff lint/format, Django system/migration checks, JSON parsing, and `git diff --check`: passed. SQLite: 97 passed with 8 PostgreSQL-only skips. Disposable PostgreSQL 14/Redis: all 105 backend tests passed after migrations, including historical source/curriculum triggers and the M2.2b review/policy regressions. Continuity and source/body/secret/generated-junk review passed. Exact-commit CI remains pending until commit/push.

## 2026-08-21 — M2.2b final review/certification validation
- Exact acquired-byte PostgreSQL operator run: official scope activated at `2d8a1052ada18b413f24b7d0eef1c855a76d8a9a31688130757d5dd6511ca56f`; V2 compiled/reconciled at `0148dea24c906e2e257265681044ae57ad4b60b9a1e290f291e95dc2315825ec`; all issue counts remained zero.
- Review workflow recorded eight Leo Rayos `APPROVE` attestations from manifest `84fec53aa090c7c7d074b0057e604ac1589917adeb26de5fde146a13dcdedeec`. The resulting human-review evidence hash is `903ab69e9171969b826060943e25d8e40fcf84d4968525663197b43165c74e44`; changed immutable review input is rejected.
- Certification created deterministic snapshot `8ffc025a-ddac-5765-b7b2-130c84282c83` / `60e160e3c1a458e4c5b98569fcf3f04d409086d328496f2ed41a020a5b591ae0`. Coverage is eight obligations, one of one pilot leaves, `PILOT_ONLY`, and `national_complete: false`; primary-authority provenance hash is `fa2a1355e70676f95fff5f02bc8c4ad03ce7594fc64be98dbf87c4cf5a0ce46c`.
- Exact operator rerun returned eight existing reviews, zero new records, and the identical snapshot. PostgreSQL trigger coverage rejects direct update and delete of human review attestations.
- Ruff lint/format and Django system/migration checks: passed. SQLite: 98 passed, 9 PostgreSQL-only skips. Disposable PostgreSQL 14/Redis: all 107 passed. All workspace checks/Web build and shared-package portability passed. Continuity, JSON, diff, and raw-source/secret review passed; exact-commit CI remains.

## 2026-08-21 — M2.2c Civil Procedure subject foundation
- Preflight: continuity passed on clean accepted main `2b042d158bb68307a0af223acec1542b62702d20`; exact-main Foundation CI run `32522925767` passed before branch creation.
- Focused checks: Ruff, Django system check, and migration consistency passed. Nine subject-plan tests passed on SQLite; one PostgreSQL-only immutable-trigger test skipped as designed.
- Focused coverage proves exact checksum-bound extraction of all six official Civil Procedure leaves; immutable/idempotent manifest and historical successor versions; 18 requirements/75 slots; 16 authority plans; required-primary versus optional-secondary hierarchy; multi-authority Erie/Hanna planning; procedural branching; treatment-aware recognition/resource distinctions; national federal/state incorporation boundary; exact Rule 4 partial-snapshot linkage without mutation; noncircular subject gate; explicit external-human review; and machine-readable operator gaps.
- Clean hash-pinned Python dependency installation passed in a disposable environment. Ruff lint/format, Django system/migration checks, 107 SQLite tests with 10 PostgreSQL-only skips, all 117 PostgreSQL 14/Redis tests, and live Celery `infrastructure.smoke` passed. The PostgreSQL-only M2.2c trigger test rejects direct subject-plan update/delete.
- Exact Node 24.19.0/npm 11.17.0 clean `npm ci` installed 717 packages. `npm run check` passed all nine typechecks, 14 Web tests, 20 native tests, 8 assessment-schema tests, and the Web production build. Seven-package portability passed. Expo dependency validation and Doctor 20/20 passed; iOS/Android production exports passed at 863/858 modules.
- `npm audit --omit=dev` remains 15 aggregate nodes (10 moderate/5 high) in the accepted `image-size` and `uuid` toolchain families; paths and mitigations are unchanged, so no security-ledger edit or forced fix occurred.
- Final continuity, JSON/manifest/checksum/milestone, diff, raw-source/secret/generated-junk, and exact-commit CI gates remain at this checkpoint.
