# BarClimb Agent Rules

Before material work, read `RECOVERY_START_HERE.md`. The repository—not conversation history—is authoritative build memory.

Do not silently change controlling product requirements. Do not begin a new material slice unless continuity validation passes. At slice completion, update current state, handoff, build history, test ledger, and any affected provider/client-parity/decision records.
